interface Routes {
  login: string;
  register: string;
  mySpace: string;
}

const endpoints: Routes = {
  login: "/login",
  register: "/sign-up",
  mySpace: "/my-space",
};

export default endpoints;
