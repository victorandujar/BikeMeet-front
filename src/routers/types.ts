interface Routes {
  login: string;
  register: string;
  mySpace: string;
  create: string;
}

const endpoints: Routes = {
  login: "/login",
  register: "/sign-up",
  mySpace: "/my-space",
  create: "/create",
};

export default endpoints;
